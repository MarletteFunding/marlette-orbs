description: >
  Sample example description.
  # What will this example document?
  # Descriptions should be short, simple, and clear.
usage:
  version: 2.1
  orbs:
    marlette-orbs: marlette-funding/marlette-orbs@0.6.0
  jobs:
    build:
      executor: python
      steps:
        - marlette-orbs/version_python:
            push_gemfury: false
            from_local: false
            requires:
              - pytest
            filters:
              branches:
                only: master
