description: >
  This command will upload to gemfury
steps:
  - restore_cache:
      keys:
        - python-sources-{{ .Branch }}-{{ .Revision }}
  - run:
      name: Upload distribution to Gemfury
      command: |
        . venv/bin/activate
        python setup.py bdist_wheel
        sh bin/deploy.sh
